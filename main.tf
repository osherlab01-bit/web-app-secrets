# infrastructure-secrets - Infrastructure as Code
terraform {
  required_version = ">= 1.0"
  
  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = "~> 5.0"
    }
  }
}

# VULNERABLE: Hardcoded AWS credentials
provider "aws" {
  region     = "us-east-1"
  access_key = "AKIAIOSFODNN7EXAMPLE"
  secret_key = "wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY"
}

# Security Group - VULNERABLE: Open to all
resource "aws_security_group" "web" {
  name        = "infrastructure-secrets-sg"
  description = "Security group for infrastructure-secrets"
  
  ingress {
    from_port   = 0
    to_port     = 65535
    protocol    = "tcp"
    cidr_blocks = ["0.0.0.0/0"]  # VULNERABLE: Open to all
  }
}
